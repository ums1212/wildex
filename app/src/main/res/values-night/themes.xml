<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 스플래시 화면용 테마 (다크 모드) -->
    <style name="Theme.Wildex.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Wildex</item>
    </style>

</resources>
